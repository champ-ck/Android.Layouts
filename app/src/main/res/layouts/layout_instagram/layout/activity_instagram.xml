<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:fresco="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context="com.iamdev.uicourse.InstagramActivity"
    android:background="#ffffff">

    <include
        android:id="@+id/tool_bar"
        layout="@layout/search_view"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentTop="true" />

    <include layout="@layout/image_circular_view"
        android:id="@+id/circular"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_margin="10dp"
        android:layout_below="@+id/tool_bar"
        android:layout_alignParentStart="true" />

    <include layout="@layout/video_view"
        android:id="@+id/video"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@+id/circular" />

    <include layout="@layout/show_image_view"
        android:id="@+id/show_image"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@+id/video" />

    <include layout="@layout/show_footer_view"
        android:layout_alignParentBottom="true"
        android:id="@+id/show_footer"
        android:layout_width="match_parent"
        android:layout_height="50dp" />


</RelativeLayout>
